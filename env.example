# App Configuration
NEXT_PUBLIC_APP_NAME=SaaSBoard
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Debug Mode (set to false in production)
DEBUG=true

# Mock Mode (set to true to use mock data instead of external services)
MOCK_MODE=true
